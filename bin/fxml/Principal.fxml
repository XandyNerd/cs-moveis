<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/21"
    xmlns:fx="http://javafx.com/fxml/1"
    fx:controller="PrincipalController"
    stylesheets="@../styles/main.css">

    <!-- MENU SUPERIOR -->
    <top>
        <MenuBar>
            <menus>
                <Menu text="Arquivo">
                    <items>
                        <MenuItem text="Sair" onAction="#sair" />
                    </items>
                </Menu>
                <Menu text="Ajuda">
                    <items>
                        <MenuItem text="Sobre" />
                    </items>
                </Menu>
            </menus>
        </MenuBar>
    </top>

    <!-- MENU LATERAL -->
    <left>
        <VBox spacing="10" prefWidth="160" styleClass="navigation-sidebar">
            <children>
                <ToggleButton text="🏠 Clientes"
                    onAction="#abrirClientes"
                    prefWidth="Infinity"
                    maxWidth="Infinity" />
                <ToggleButton text="📦 Produtos"
                    onAction="#abrirProdutos"
                    prefWidth="Infinity"
                    maxWidth="Infinity" />
                <ToggleButton text="💰 Vendas"
                    onAction="#abrirVendas"
                    prefWidth="Infinity"
                    maxWidth="Infinity" />

                <!-- Spacer to push logout button to bottom -->
                <Region VBox.vgrow="ALWAYS" />

                <Button text="🚪 Sair"
                    onAction="#sair"
                    styleClass="secondary"
                    prefWidth="Infinity"
                    maxWidth="Infinity" />
            </children>
        </VBox>
    </left>

    <!-- CONTEÚDO CENTRAL -->
    <center>
        <AnchorPane fx:id="contentPane" styleClass="card" />
    </center>

</BorderPane>